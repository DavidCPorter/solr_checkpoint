# playbook for cloudconfig
- hosts: all,!localhost
  roles:
    - cloud-env

- hosts: generatorNode
  tasks:
    - name: pip install numpy
      pip:
        name: numpy
        executable: /usr/bin/pip3
